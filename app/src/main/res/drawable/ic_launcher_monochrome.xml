<!-- The monochrome layer preserves the same eight true trigram patterns. -->
<inset xmlns:android="http://schemas.android.com/apk/res/android"
    android:inset="0dp"
    android:drawable="@drawable/bagua_monochrome" />
